import React from 'react';
import { COLORS } from '../lib/colors';

export default function AppNameComponent() {
    return (
        <span style={{ fontWeight: '500', fontSize: '20px', lineHeight: '36px', color: COLORS.textPrimary }}>
            Varisankya
        </span>
    );
}
